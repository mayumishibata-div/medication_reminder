<!DOCTYPE html>
<html>
<head>
  <title>MedicationReminder</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body class="bg-gray-100 font-sans antialiased">
  <header class="bg-orange-400 text-white shadow-md">
    <div class="container mx-auto px-4 py-4 flex items-center justify-between">
      <div class="flex items-center">
        <%= link_to root_path, class: "flex items-center space-x-2 text-white hover:text-orange-100 transition duration-200" do %>
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="w-7 h-7">
            <path stroke-linecap="round" stroke-linejoin="round" d="M9 12h3.75M9 15h3.75M9 18h3.75C10.707 18 12 16.707 12 15s-1.293-3-3-3m0-6V4.774M21 12a9 9 0 1 1-18 0 9 9 0 0 1 18 0Z" />
          </svg>
          <span class="text-xl md:text-2xl font-bold">薬のミカタ</span>
        <% end %>
      </div>

      <nav>
        <ul class="flex items-center space-x-4 md:space-x-6">
          <% if user_signed_in? %>
            <%# <li><%= link_to 'マイ薬一覧', medications_path, class: "text-white hover:text-orange-100 px-3 py-2 rounded-md transition duration-200" %></li> %>
            <%# <li><%= link_to '服用記録', doses_path, class: "text-white hover:text-orange-100 px-3 py-2 rounded-md transition duration-200" %></li> %>
            <li><%= link_to '設定', edit_user_registration_path, class: "text-white hover:text-orange-100 px-3 py-2 rounded-md transition duration-200" %></li>
            <li><%= link_to 'ログアウト', destroy_user_session_path, data: { turbo_method: :delete, turbo_confirm: '本当にログアウトしますか？' }, class: "bg-orange-600 hover:bg-orange-800 text-white font-semibold px-4 py-2 rounded-md transition duration-200" %></li>
          <% else %>
            <li><%= link_to '新規登録', new_user_registration_path, class: "text-white hover:text-orange-100 px-3 py-2 rounded-md transition duration-200" %></li>
            <li><%= link_to 'ログイン', new_user_session_path, class: "bg-orange-600 hover:bg-orange-800 text-white font-semibold px-4 py-2 rounded-md transition duration-200" %></li>
          <% end %>
        </ul>
      </nav>
    </div>
  </header>

  <p class="notice text-green-700 bg-green-100 p-3 text-center"><%= notice %></p>
  <p class="alert text-red-700 bg-red-100 p-3 text-center"><%= alert %></p>

  <main class="container mx-auto px-4 py-8">
    <%= yield %>
  </main>

</body>
</html>